<?xml version="1.0" encoding="utf-8"?>
<!--
  1 section trong Genre Detail: tiêu đề (Bold 16sp, trắng, padding top 24dp) +
  RecyclerView ngang. Gộp header + content vào 1 row (giống item_home_section.xml)
  để tránh header/content lệch nhau khi RecyclerView recycle.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tv_section_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="24dp"
        android:paddingBottom="12dp"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_section_items"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:nestedScrollingEnabled="false"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"/>
</LinearLayout>
