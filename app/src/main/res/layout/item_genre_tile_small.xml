<?xml version="1.0" encoding="utf-8"?>
<!-- Tile genre nhỏ cho hàng cuộn ngang (RELATED_GENRES). Nhỏ hơn item_genre_tile.xml. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="72dp"
    android:layout_marginEnd="10dp"
    android:layout_marginBottom="6dp"
    android:background="@drawable/bg_genre_tile">

    <TextView
        android:id="@+id/tv_genre_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="10dp"
        android:textColor="@color/accent_white"
        android:textSize="13sp"
        android:textStyle="bold"
        android:maxLines="2"
        android:ellipsize="end"/>

    <ImageView
        android:id="@+id/iv_genre_pic"
        android:layout_width="44dp"
        android:layout_height="50dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="4dp"
        android:layout_marginBottom="-2dp"
        android:rotation="25"
        android:scaleType="centerCrop"
        android:src="@drawable/placeholder_gradient"/>
</FrameLayout>
